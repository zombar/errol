ollama:
  host: http://localhost:11434
  timeout: 300

models:
  planning: mistral-small3.2:24b
  writing: gpt-oss:20b

agent:
  max_turns: 75
  max_todos: 5  # Max todo items before asking user for preferred count
  validation:
    enabled: true  # Run validation after all tasks completed
    model: null    # Use specific model for validation (null = use writing model)
